version: "3.8"

services:
  fanhist:
    image: ghcr.io/grassiekuik/fanhist:latest
    # or build from source instead of pulling: build: .
    container_name: fanhist
    restart: unless-stopped
    ports:
      - "8181:8081"
    volumes:
      - ./data:/data
